import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  novaLeituraTexto: {
    margin: 0,
    fontSize: 10,
    color: '#fff',
    textAlign: 'center',
  },
  noDeviceFoundText: {
    textAlign: 'center',
    fontSize: 26,
  },
});

export default styles;
